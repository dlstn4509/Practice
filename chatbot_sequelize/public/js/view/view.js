let btnTring = document.querySelector('.btn-tring');

btnTring.addEventListener('click', imgTring);

let turnAngle = 90;
function imgTring() {
  let img = document.querySelector('.thumbImg');
  // img.setAttribute('style', 'transform: rotate(' + turnAngle + 'deg)');
  // turnAngle = turnAngle + 90;
  // sharp(img).rotate(90);
}
